# Buyali V1.3.2

V1.3.2 主要更新 Windows 数据采集助手，集中改善识别准确率、角色信息展示、配对与同步稳定性，并统一助手界面风格。

## 本次更新

### 数据采集更准确

- 修复白奥德、蓝奥德或基纳为 `0` 时无法正确采集的问题。
- 降低基纳被相邻货币数值干扰的概率。
- 进一步区分战斗力与道具等级，避免带 `K` 的战斗力被写入道具等级。
- 改善不同游戏背景、分辨率、DPI、单屏和多屏环境下的识别兼容性。
- 当识别角色与写入目标不一致时阻止自动写入，避免数据写错角色。

### 采集助手界面升级

- 统一为 Buyali 深色界面与青色高亮风格，缩小弹窗和角色信息面板占用面积。
- 相机入口左键采集数据、右键查看角色信息；识别过程直接在入口显示加载状态。
- 角色信息面板可查看职业、角色、账号、道具等级及白奥德（+蓝奥德）。
- 角色默认按剩余奥德从高到低排列，一次显示最多 6 个角色，并支持鼠标滚轮浏览。
- 支持仅显示当前角色所属账号的角色。
- 优化任务栏、系统托盘和控制面板入口。

### 配对、同步与更新

- 完善首次配对、配对失效和重新绑定流程。
- 确认采集时显示变更前与变更后数据，变化值以黄色标注，并允许手动修正。
- 写入成功后保留 8 秒撤回入口，一次采集写入对应网页的一次撤回记录。
- 修复采集写入后本地数据与云端数据基线不同步的问题。
- 新增采集助手自动更新能力；更新失败不会影响当前版本继续使用。

### 安装包与隐私

- 正式包采用扁平结构，完整解压后可直接运行 `BuyaliCollector.exe`。
- 原始截图仅在本机内存中用于识别，不上传、不长期保留。

## 首次使用

1. 完整解压采集助手压缩包。
2. 运行 `BuyaliCollector.exe`。
3. 在 Buyali 网页打开“连接数据采集助手”，生成 6 位配对码。
4. 在采集助手中输入配对码完成连接。
5. 游戏中左键相机入口采集数据，右键查看角色信息。

## 更新说明

- 建议退出旧版采集助手后，再完整解压并运行 V1.3.2。
- 请勿直接在压缩包内运行程序。
- 如需切换 Buyali 用户或设备，可通过控制面板执行“重新绑定”。

## 已知提示

- 当前程序尚未使用商业代码签名证书，Windows 或部分安全软件可能显示未知发布者提示。
- 如果安全软件拦截 `buyali.xyz`，需要将网站加入信任列表；网站信誉申诉属于独立处理事项。

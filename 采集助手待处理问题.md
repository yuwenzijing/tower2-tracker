# 数据采集助手待处理问题

记录日期：2026-08-05  
处理范围：仅收集与分析，本轮不修改、不打包、不发布采集助手。

## COL-01 奥德识别遗漏

- 界面中奥德显示为 `0(+1,995)/840` 时，确认窗口只出现基纳，没有白奥德和蓝奥德。
- 需要兼容“当前值 + 额外值 / 上限”的显示形式，并分别验证白奥德、蓝奥德的字段映射。

## COL-02 多识别点准确率不稳定

- 基纳、战斗力、道具等级曾出现截断、串位或选中相邻货币的问题。
- 已确认部分 OCR 文本本身正确，错误发生在候选区域与字段归属阶段。
- 后续应统一检查所有识别点的锚点定位、候选排序、跨帧一致性和数值范围校验，不能继续按场景颜色逐张穷举。

## COL-03 ZIP 内直接运行失败

- 用户直接从压缩包临时目录启动时，出现 `python312.dll` 加载失败。
- 安装包或下载说明需要阻止/明确提示“先完整解压”，并检查发布压缩包根目录名称及依赖完整性。

## COL-04 网站与程序安全信誉

- `buyali.xyz` 被 Trend Micro 家庭网络安全标记为危险页面；Edge 同时显示连接不安全警告。
- 需要分别核查域名信誉申诉、TLS 证书链、站点响应头、下载文件签名与杀毒平台误报情况。
- 此问题可能影响用户访问网页及下载/启动采集助手，但不能仅靠引导用户关闭安全防护解决。

## COL-05 采集确认界面可用性

- 确认弹窗在部分分辨率和摆放位置下尺寸较大，可能遮挡游戏内容。
- 本轮不调整；后续与 OCR 修复一起回归单屏、双屏及不同 DPI。

## 与网页同步问题的边界

- “数据不一致”弹窗属于网页端本地缓存与云端版本缺少共同基线的问题，本轮已在网页端修复。
- 采集助手成功写入云端后，网页会以事件和云端版本为准更新本地；采集助手本身无需因此重新打包。

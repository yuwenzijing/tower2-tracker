# 更新日志

## V1.3.4.1 (2026-08-29)

### 基纳识别一致性

- 修复相同货币栏在物质变换界面与普通 HUD 中可能得到不同基纳结果的问题。
- 当放大 OCR 将 `438,589,356` 截断为 `438,589,36` 时，自动使用原始倍率复核，并以完整、可信度更高的结果为准。
- 复核仅在发现千分位数字残缺时触发，不增加普通采集场景的 OCR 次数。
- 两张问题截图现均稳定识别基纳为 `438,589,356`，不会误取相邻的 `433,589,356`。

## V1.3.4 (2026-08-29)

### 自动更新修复

- 修复版本比较只读取前三段数字、导致 `1.3.3` 无法发现 `1.3.3.1` 等热修复版本的问题。
- 版本比较现支持全部数字段，后续四段及更多段版本号均可正常检测。
- V1.3.4 使用三段版本号发布，使现有 V1.3.3 客户端能够检测并触发本次自动更新。

### 识别修复

- 包含 V1.3.3.1 的基纳识别热修复：普通 HUD 中 `3,338` 等前置紫色货币不再被误识别为基纳。

## V1.3.3.1 (2026-08-29)

### 识别修复

- 修复普通 HUD 中紫色货币达到四位数并显示千分位时，可能被误识别为基纳的问题。
- 当货币栏显示 `3,338 / 433,589,356 / 433,589,356` 且部分候选被 OCR 漏读时，现在会跳过前置紫色货币并正确选择 `433,589,356` 作为基纳。
- 保留 V1.3.3 对 `190,418,686 / 167,517,846 / 13,706,005` 场景的识别规则，并新增两项布局回归与一项大额货币顺序回归测试。

## V1.3.3 (2026-08-24)

### 识别与性能

- 修复普通 HUD 同时显示三种货币时，基纳被错误归属为相邻货币的问题；截图 `190,418,686 / 167,517,846 / 13,706,005` 现在选择第一项基纳。
- 修复游戏以 `45/840` 等形式省略 `+0` 时蓝奥德未进入更新字段的问题；完整的 `白奥德/840` 现在会明确写入蓝奥德 `0`。
- 采集点击后的后台任务启动等待由 160ms 降至 20ms，并记录截图、字段 OCR、角色 OCR及确认界面前总耗时。
- 最新角色数据读取与本地 OCR 并行；游戏窗口标题已有可靠角色名时跳过额外角色名 OCR。
- OCR 引擎改为界面显示后的后台初始化，并增加线程安全保护，避免重复初始化。
- 角色信息浮窗优先在 20ms 响应路径显示上次缓存，并立即在后台校验；无缓存时显示加载状态。打开期间可配置每 2 秒校验，内容未变化时不重绘。
- 网络失败时允许显示明确标记时间的缓存，不把缓存伪装为最新数据。
- 角色数据自动更新与软件版本更新拆分为独立状态；设置浮层可单独开关同账号筛选和角色数据自动更新。
- 缓存职业图标并按系统 DPI 缩放，避免每次右键打开浮窗都重新读取资源或在高分屏下显示过小。

### 代码整理

- 正式构建版本和安装包标识更新为 `1.3.3`，连接正式环境并沿用独立配置目录与自动更新通道。
- 将桌面端颜色、字体和职业名称等视觉变量集中到独立主题模块，为逐界面自适应改版做准备。
- 增加基纳归属、快速任务启动、并行取数、角色 OCR 跳过条件及角色浮窗新鲜度回归测试。

### 界面更新

- 完成 UI-01～UI-14 全套界面验收稿在采集助手测试版中的落地，统一任务栏主窗口、配对、绑定、提示、采集确认、写入成功及撤回等界面的视觉语言和自适应规则。
- 主窗口改为紧凑状态卡片布局；配对与角色绑定流程改为分步信息结构，减少重复字段和无效留白。
- UI-12 采集确认表格改为“数据项 / 变更前 / 变更后 / 变化”四列，变化值按正数、负数和无变化显示，并随人工修正实时更新。
- UI-12 的变化列仅用于展示；实际写入继续使用可编辑的“变更后”数据，角色一致性、识别可靠性和异常变动等自动写入保护保持不变。
- 删除角色信息浮窗顶部短暂变化的“自动更新 · 刚刚同步/检查中”文字；后台仍每 2 秒检查，并且仅在真实数据变化时重绘。
- 所有界面使用真实职业图标和角色字段，完成测试环境验收后随 V1.3.3 正式发布。
- 网页角色卡新增单行备注：仅选中角色显示入口，支持 Enter/点击外部保存、Esc 取消、清空删除；长备注在悬停时滚动，且不会挤压每周待办标题或增加卡片高度。

## V1.3.2 (2026-08-11)

### 数据采集助手

- 全面调整采集助手界面，统一为 Buyali 在线版的深色、青色高亮风格，并压缩确认、成功、失败、绑定、设置及角色信息等窗口的占用面积。
- 浮窗入口改为相机图标：左键开始数据采集，右键打开角色信息面板；识别期间直接在入口图标显示加载状态，减少对游戏画面的遮挡。
- 新增角色信息面板，紧凑展示“职业、角色、账号、道具等级、白奥德（+蓝奥德）”，按剩余奥德从高到低排列，一次最多显示 6 个角色并支持鼠标滚轮浏览。
- 角色信息面板新增“只显示同账号角色”选项；职业名称和职业图标直接读取网页数据，未设置职业时仅显示角色名，不再重复显示“未设置”。
- 新增自动更新能力，可检测新版本、下载更新包，并在重启后完成替换；更新失败时保留当前可用版本。
- 优化任务栏与系统托盘入口，保留显示/隐藏浮窗、重置浮窗、重新绑定、取消当前采集和退出功能。
- 发布包改为扁平目录结构，解压后可直接在首层运行 `BuyaliCollector.exe`，减少多层目录带来的使用成本。

### 识别准确率与兼容性

- 修复白奥德或蓝奥德为 `0` 时被当作“未识别”的问题，兼容 `0(+额外值)/840` 等游戏显示形式。
- 优化基纳候选区域和货币顺序判断，避免基纳为 `0` 时误读取相邻货币数值。
- 优化战斗力与道具等级的区分：带 `K` 的数值按战斗力处理，不带 `K` 的 3～4 位数按道具等级处理。
- 优化顶部资源栏、角色面板等识别区域在不同副本背景、亮度、分辨率、DPI 和多显示器环境中的兼容性。
- 识别角色与网页绑定角色不一致时禁止自动写入，重新提示用户更换绑定或人工确认，避免写入错误角色。
- 优化截图与 OCR 的后台执行流程，减少采集瞬间对游戏画面的影响，并保留超时自动恢复和取消采集能力。

### 配对、写入与同步

- 修复测试修订版本号导致采集助手误连正式接口、配对码始终无效的问题。
- 完善首次配对、配对失效和重新绑定流程；未配对时点击浮窗会直接进入配对，而不是显示模糊的令牌错误。
- 采集确认窗口显示变更前、变更后数据；变化值使用黄色标注，无变化值保持普通文字，并允许写入前手动修正。
- 采集成功仍作为一个完整事务写入：网页撤回步数增加 1；成功窗口保留 8 秒撤回入口，并可立即确认关闭。
- 修复采集写入后网页本地数据与云端数据不同步、频繁出现“数据不一致”提示的问题，采集事件会同步更新本地版本基线。
- 修复离线版导入时 `simpleMode is not defined` 的错误，并同步检查在线版导入、导出逻辑。

### 使用与隐私

- 原始截图仅在本机内存中用于识别，不上传、不长期保存。
- V1.3.2 正式版继续支持在线版采集写入；离线版本保留本地保存与导入、导出能力。

## V1.3.1 (2026-08-04)
- 新增 Windows 便携式数据采集助手，无需安装，可在游戏界面使用可拖动浮窗采集数据。
- 支持识别当前角色、白奥德、蓝奥德、基纳、战斗力和道具等级；按当前界面存在的数据单次采集。
- 采集确认窗口显示变更前与变更后数据，发生变化的结果以黄色标注，并支持手动修正。
- 识别角色与网页角色唯一匹配时直接进入数据确认；不匹配时可绑定已有角色或创建并绑定新角色。
- 确认窗口 5 秒后自动写入；写入成功后 8 秒内可撤回，并提供立即关闭按钮。
- 助手增加任务栏入口、状态面板、显示/隐藏浮窗、重置位置、重新绑定和退出功能。
- 优化多显示器、游戏窗口切换、弹窗定位及 OCR 对不同副本背景与颜色的兼容性。
- 移除网页中原有的单角色浏览器截图按钮，改用外置采集助手。
- 隐私：原始截图不上传、不长期保存，识别完成后即清理。

## V1.3.0 (2026-08-03)
- 新增：截图 OCR 数据采集（选中角色后点击相机按钮，一键屏幕捕获 + 自动识别）
- 新增：OCR 自动识别白奥德、蓝奥德、基纳、战斗力，识别结果可手动修正后确认应用
- 新增：账号级「战场三连」待办（每周三 3:00 重置）
- 新增：账号级「每日副本」待办（每周三 3:00 重置）
- 新增：「单次副本消耗」全局切换（精简模式旁），会员 80↔120、非会员 40↔60 一键切换
- 优化：账号级待办单行流式布局，指令书/战场/副本用分隔符区分层级
- 优化：云同步冲突确认弹窗显示当前同步口令，避免多设备切换时混淆
- 优化：截图完成后自动断开屏幕捕获，不再持续占用
- 优化：精简模式切换保持滚动位置，不再跳回顶部
- 技术：百度云端 OCR 高精度识别，Cloudflare Worker 代理 API 调用
- 技术：截图自动压缩（JPEG 0.75、最大 1920px），兼容各分辨率屏幕
- 注意：本地版不含同步和 OCR 功能，保留保存按钮

## V1.1.5 (2026-07-27)
- 新增：角色卡片选中状态，点击非编辑区域选中角色
- 新增：选中角色显示能量快捷操作按钮（-120/-80/+40/+10/撤回），支持白蓝奥德联动扣减
- 新增：能量操作独立撤回（每角色最多5步），与页面级撤回互不干扰
- 新增：能量操作按钮 100ms 冷却防连点
- 新增：页面级「撤回」按钮移至全局工具栏（☁同步与导出数据之间）
- 优化：同步状态指示器精简，☁同步按钮颜色直接表示状态（绿=已同步/蓝=同步中/红=失败）
- 优化：精简模式点击区域缩小至仅复选框，防止误触
- 优化：白奥德/蓝奥德卡片仅输入框阻止角色选中，点击卡片其他区域可正常触发选中
- 优化：角色名输入框宽度自适应内容，最小宽度2个中文字符
- 优化：所有编辑操作即改即存，无需手动点击保存
- 精简：CSS 合并数字输入框/弹窗遮罩层重复规则
- 精简：JS 统一待办函数为配置驱动（TODO_CONFIG），提取 findChar/getWeeklyResetPoint 消除重复代码
- 新增：能量撤回按钮显示剩余可撤回步数
- 注意：本地版保留保存按钮（已移至全局工具栏），不含撤回和同步功能

## V1.1.3 (2026-07-24)
- 新增：今日活动模块（根据活动配置自动显示当天活动，5分钟内开启变红提醒，支持滚动）
- 新增：账号维度「指令书」待办（亚尔特盖德/莫尔海姆/深渊下层/深渊中层，每周三3:00重置）
- 新增：基纳统计旁直接显示基纳总额（千分号格式，透明度降低避免喧宾夺主）
- 新增：云同步弹窗「手动推送」按钮（自动同步失败时的备用手段）
- 新增：活动配置独立为 JSON 文件，同时内嵌默认值兼容本地文件打开
- 优化：标题栏精简，移除副标题，版本号改为页内小字显示
- 优化：浏览器标签页标题不再显示版本号
- 优化：云同步弹窗改版（关闭按钮移至右上角，底部4按钮等宽排列，口令改为明文显示）
- 优化：指令书勾选状态走「保存」按钮流程，与角色待办操作一致
- 优化：指令书勾选样式统一为绿勾+绿字（与角色待办视觉一致）
- 优化：白奥德上限角色改为竖向排列，单列最多3个
- 同步：指令书数据纳入云同步和导入导出

## V1.1.2 (2026-07-23)
- 新增：云同步功能（自定义口令跨设备同步数据）
- 新增：同步口令修改按钮，操作更便捷
- 新增：云端数据 30 天未同步自动清除
- 新增：首次设置口令时，云端已有数据的排重提示
- 新增：数据冲突弹窗，显示双方修改时间和账号数供用户选择
- 部署：Cloudflare Workers + KV 存储，自定义域名 buyali.xyz

## V1.1.1 (2026-07-20)
- 优化：蓝奥德卡片渐变效果（从左侧边框向右淡出）
- 优化：感叹号样式改为非按钮样式（青色、低透明度）
- 优化：角色卡属性输入框左移，避免与删除按钮重叠
- 优化：编辑属性输入框时禁用角色卡拖拽
- 优化：基纳统计弹窗兼容性（显式 display 控制）
- 修复：白奥德和蓝奥德渐变方向统一为从左到右

# Buyali V1.3.3 更新日志

发布日期：2026 年 8 月 24 日

V1.3.3 完成了数据采集助手的整体界面升级，并重点改善识别准确率、采集响应速度和角色信息浮窗体验。网页版同时新增角色备注功能。

## 网页备注

- 选中角色后，可在“每周待办”同一行添加单行备注。
- 无备注时仅显示铅笔图标，不占用额外高度。
- 支持按 `Enter` 或点击输入框外部保存，按 `Esc` 取消编辑。
- 清空备注后保存即可删除。
- 长备注保持单行显示；超过 20 个字符时截断，鼠标悬停后滚动查看全文。
- 备注文字使用黄色提醒色，编辑入口保持低强调灰色并在悬停时变为青色。
- 备注随现有网页数据一起保存和同步。

## 采集助手界面升级

- 完成任务栏主窗口、浮窗入口、角色信息面板、配对、绑定、采集确认、写入成功、撤回和提示窗口的统一改版。
- 新界面继续沿用 Buyali 网页版的深色背景、青色强调和真实职业图标。
- 优化高 DPI、多显示器和不同窗口尺寸下的自适应显示。
- 角色信息浮窗移除重复职业文字，压缩无效留白，并优化角色、账号、道具等级和奥德列的对齐。
- 角色信息在后台每 2 秒检查一次，仅在数据真实变化时重绘；不再显示频繁变化的“刚刚同步”文字。
- UI-12 采集确认窗口新增“变化”列，清晰展示每项数据的增加、减少或无变化。
- “变化”列仅用于提示，实际写入仍以可编辑的“变更后”数据为准。

## 识别准确率

- 修复普通 HUD 同时显示多种货币时，基纳被错误识别为相邻货币的问题。
- 示例画面中的基纳现在可正确识别为 `190,418,686`，不会误取 `167,517,846`。
- 修复蓝奥德为 `0` 时被当作未识别、导致网页数据不更新的问题。
- 兼容游戏省略 `+0`、仅显示 `45/840` 等奥德格式的情况。
- 游戏窗口标题已提供可靠角色名时，跳过额外角色名 OCR，减少错误与等待。

## 性能与稳定性

- 采集点击后的后台任务启动等待由约 160ms 缩短至约 20ms。
- 最新角色数据读取与本地 OCR 并行进行，缩短确认窗口出现时间。
- OCR 引擎在界面显示后于后台初始化，并增加线程安全保护，避免重复初始化造成卡顿。
- 右键打开角色信息浮窗时优先显示缓存，再在后台校验最新数据。
- 无缓存时显示明确加载状态；网络失败时只展示带时间标记的缓存，不将旧数据显示为最新数据。
- 缓存职业图标和缩放结果，减少浮窗重复打开时的磁盘读取和图像处理。

## 安装与更新

1. 前往 [V1.3.3 Release](https://github.com/yuwenzijing/tower2-tracker/releases/tag/v1.3.3)。
2. 下载 `BuyaliCollector-v1.3.3.zip`。
3. 完整解压压缩包，不能直接在压缩包预览窗口中运行。
4. 打开 `BuyaliCollector-v1.3.3` 文件夹并运行 `BuyaliCollector.exe`。

旧版本用户也可使用采集助手内置的自动更新功能。更新失败不会破坏当前可用版本。

## 隐私说明

采集助手仅在本机内存中截取当前 AION2 游戏窗口用于识别。原始截图不会上传，也不会长期保存；确认后只向已配对的 Buyali 数据写入结构化数值。

## 已知提示

- 当前 Windows 程序尚未使用商业代码签名证书，Windows SmartScreen 或部分安全软件可能显示“未知发布者”。
- 请只通过 Buyali 正式网页或本项目 GitHub Release 下载采集助手。


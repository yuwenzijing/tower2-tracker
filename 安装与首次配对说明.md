# Buyali 数据采集助手 V1.3.1：安装与首次配对说明

## 一、下载与启动

1. 打开 `https://buyali.xyz`，点击页面工具栏中的“数据采集”。
2. 点击“下载 Windows 数据采集助手 V1.3.1”，下载 `BuyaliCollector-v1.3.1.zip`。
3. 将压缩包完整解压到任意文件夹。不要只把 EXE 单独拖出，程序目录和 `runtime` 目录需要保持在一起。
4. 双击 `BuyaliCollector/BuyaliCollector.exe`。程序无需安装，启动后会在任务栏显示入口。

Windows 首次运行若显示安全提示，请确认文件来自 buyali.xyz 对应的 GitHub Release，再选择继续运行。

## 二、首次配对

1. 登录 `buyali.xyz`，并确认网页已设置云同步口令。
2. 点击网页工具栏中的“数据采集”，网页会显示一个 6 位一次性配对码，有效期为 5 分钟。
3. 在采集助手中输入该配对码并确认。
4. 助手显示“已配对”后即可关闭网页；后续采集可由助手直接写入云端。

配对只需要完成一次。更换同步口令、切换到其他网页数据或主动选择“重新绑定”时，需要重新配对。

## 三、采集数据

1. 启动 AION2，并切换到需要采集的角色。
2. 打开能显示目标数据的游戏界面：奥德与基纳界面，或显示战斗力/道具等级的角色界面。
3. 点击游戏窗口上的“采集数据”浮窗按钮。
4. 核对识别角色、写入位置及变更前后数据。黄色数字表示本次将发生变化。
5. 选择“立即写入”，或等待 5 秒自动写入。写入后 8 秒内可撤回本次写入。

一个游戏界面没有展示的字段不会被修改。战斗力带 `K`，网页按 `K` 数值保存；道具等级不带 `K`，按整数保存。

## 四、常用操作

- 浮窗可直接拖动。
- 从任务栏入口可显示/隐藏浮窗、重置浮窗位置、重新绑定或退出程序。
- 若浮窗在多显示器环境中不可见，先使用“重置浮窗位置”，再切回游戏。
- 若识别结果不正确，请先取消，不要写入；确认打开了正确的数据界面后重新采集。

## 五、隐私说明

助手只截取当前游戏窗口用于本地识别，不上传原始截图，也不长期保留截图。确认后仅将结构化数值写入已配对的 Buyali 云端数据。

# Buyali 数据采集助手界面与流程清单

版本：V1.3.1 正式版
用途：用界面编号反馈裁切、重叠、跳转或功能问题。

## 一、界面编号

### UI-01 主控制面板

- 显示：配对状态、游戏窗口连接状态、当前识别角色、浮窗位置。
- 操作：显示/隐藏浮窗、重新绑定、取消当前采集。
- 关闭窗口：仅最小化，不退出程序。
- 更多操作由 UI-02 承担，避免主面板按钮堆叠。

### UI-02 系统托盘右键菜单

- 入口：Windows 任务栏右下角的 BUYALI 图标。
- 操作：显示控制面板、显示/隐藏浮窗、取消当前采集、重置浮窗、重新绑定、退出。
- 双击默认操作：显示 UI-01。

### UI-03 游戏采集浮窗

- 显示：`采集数据`、`正在识别…`两种状态。
- 操作：单击采集；拖动调整位置；右键显示 UI-01。
- 截图前暂时隐藏，处理结束后重新显示。

### UI-04 配对码输入窗口

- 首次启动或重新绑定时出现。
- 输入网页生成的六位配对码。
- 操作：确认、取消。
- 成功进入 UI-05；失败进入 UI-06。

### UI-05 配对成功提示

- 显示采集助手已连接正式环境。
- 操作：确定。

### UI-06 通用提示窗口

- 用于：未找到游戏、采集失败、连接失败、输入错误、撤回结果等。
- 显示：标题、说明、确定按钮。
- 这是共用模板，不代表多个不同界面。

### UI-07 当前游戏角色输入窗口

- 仅在窗口标题和绿色角色名都没有识别结果时出现。
- 绿色角色名能够匹配唯一网页角色时，跳过本界面并直接进入 UI-09。
- 识别到新角色名时，跳过本界面并直接进入 UI-08 创建/绑定。
- 显示绿色文字识别候选，并允许人工输入当前角色名。
- 操作：确认、取消。

### UI-08 网页角色绑定窗口

- 仅在无法按同名角色自动匹配、历史绑定无效，或用户主动更换绑定时出现。
- 操作：从列表绑定已有角色；或直接选择目标账号，将识别到的游戏角色名创建并绑定；取消。
- 不再要求手工输入账号名称或再次输入角色名称。

### UI-09 确认采集内容窗口

- 显示：识别角色、实际写入位置、数据项、变更前数值、变更后数值、可信度提示、5 秒倒计时。
- 数值未变化：变更前和变更后均使用正常字体。
- 数值有变化：仅变更后数值使用黄色；人工修改时颜色实时更新。
- 操作：更换绑定、立即写入、取消。
- 角色不一致或字段可信度低时暂停自动写入。

### UI-10 采集成功及撤回窗口

- 显示：当前角色、已写入字段、8 秒倒计时。
- 操作：撤回本次写入。
- 8 秒结束后自动关闭；撤回只针对本次采集事务。

### UI-11 创建新角色时的账号输入窗口

- 从 UI-08 的“创建新角色”进入。
- 显示已有账号名称，输入目标账号名称。
- 操作：确认、取消。

## 二、完整流程编号

### FLOW-01 启动与配对

1. 启动程序，显示 UI-01、UI-02、UI-03。
2. 未配对：UI-04 → UI-05 或 UI-06。
3. 已配对：直接检测游戏窗口和当前角色。

### FLOW-02 正常采集

1. 点击 UI-03。
2. UI-03 显示“正在识别…”，随后暂时隐藏。
3. 截取游戏窗口，不上传、不保留原始截图。
4. 识别当前画面的有效字段：
   - 远征界面：白奥德、蓝奥德（存在时）、基纳。
   - HUD 道具模式：道具等级、基纳。
   - HUD 战力模式：战斗力、基纳。
5. 解析角色：自动匹配成功直接继续；无法匹配时进入 UI-07/UI-08。
6. 显示 UI-09。
7. 倒计时结束或点击“立即写入”。
8. 写入云端并生成事务编号。
9. 显示 UI-10；8 秒内允许撤回本次事务。
10. 结束后恢复 UI-03。

### FLOW-03 更换角色绑定

1. 在 UI-09 点击“更换绑定”。
2. 进入 UI-08。
3. 选择已有角色或通过 UI-11 创建角色。
4. 返回 UI-09，重新显示写入位置。

### FLOW-04 取消与异常恢复

1. 识别期间可在 UI-01 或 UI-02 选择“取消当前采集”。
2. 超时、无有效数据或网络错误进入 UI-06。
3. 确认提示后恢复 UI-03，不应停留在“正在识别…”。

## 三、反馈格式

请使用以下格式反馈，便于准确定位：

`界面编号 + 分辨率/缩放比例 + 问题位置 + 期望结果`

示例：`UI-09，3840×2160、Windows 150% 缩放，底部“更换绑定”被裁切，期望三按钮完整显示。`

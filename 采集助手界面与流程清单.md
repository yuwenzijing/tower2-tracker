# Buyali 数据采集助手界面与流程清单

版本：V1.3.3-test 已验收实现基线
用途：用界面编号反馈裁切、重叠、跳转或功能问题。

> V1.3.2 已将角色信息、设置和自动更新界面加入助手，因此下方 V1.3.1 编号不再作为完整清单。V1.3.3 的 Product Design 逐界面确认采用以下现行编号。

## 零、V1.3.3 当前完整 UI 基线

- UI-01：Windows 任务栏主控制窗口。
- UI-02：系统托盘图标与右键菜单。（Precision Lens 方案已实现）
- UI-03：游戏内采集浮窗及默认、识别中、隐藏状态。（Precision Lens 方案已实现）
- UI-04：右键角色信息浮窗。（已验收并进入测试版实现）
- UI-05：角色信息设置浮层。（已验收并进入测试版实现）
- UI-06：六位配对码输入窗口。
- UI-07：配对成功、失效及重新连接提示。
- UI-08：成功、失败、警告等通用消息窗口。
- UI-09：自动更新已就绪窗口。
- UI-10：当前游戏角色人工确认窗口。
- UI-11：网页角色绑定及创建窗口。
- UI-12：采集内容确认窗口。
- UI-13：采集成功及限时撤回窗口。
- UI-14：所有界面共用的标题栏、按钮、输入框、滚动条、状态色和自适应规则。

验收状态：UI-01～UI-14 已全部通过，当前仅推进 `1.3.3-test` 本地测试版；本地验收通过前不发布 GitHub、不影响正式环境。

## 一、界面编号

### UI-01 主控制面板

- 显示：配对状态、游戏窗口连接状态、当前识别角色、浮窗位置。
- 操作：显示/隐藏浮窗、重新绑定、取消当前采集。
- 关闭窗口：仅最小化，不退出程序。
- 更多操作由 UI-02 承担，避免主面板按钮堆叠。

### UI-02 系统托盘右键菜单

- 入口：Windows 任务栏右下角的 BUYALI 图标。
- 操作：显示控制面板、显示/隐藏浮窗、取消当前采集、重置浮窗、重新绑定、退出。
- 双击默认操作：显示 UI-01。

### UI-03 游戏采集浮窗

- 显示：`采集数据`、`正在识别…`两种状态。
- 操作：单击采集；拖动调整位置；右键显示 UI-01。
- 截图前暂时隐藏，处理结束后重新显示。

### UI-04 右键角色信息浮窗

- 显示：职业图标、角色名、账号、道具等级、白奥德与蓝奥德。
- 操作：按奥德排序、滚轮浏览、打开 UI-05、关闭。
- 性能：先显示缓存，再后台检查最新数据；仅数据变化时重绘。

### UI-05 角色信息设置浮层

- 操作：只显示同账号角色、自动更新角色数据。
- 显示：检查频率、仅变化重绘说明、上次更新时间、版本号。
- 自适应：跟随 UI-04 放置，空间不足时自动换到另一侧。

### UI-06 配对码输入窗口

- 首次启动或重新绑定时出现。
- 输入网页生成的六位配对码。
- 操作：确认、取消。
- 成功进入 UI-07；失败进入 UI-08。

### UI-07 配对成功、失效及重新连接提示

- 显示采集助手已连接正式环境。
- 操作：确定。

### UI-08 通用提示窗口

- 用于：未找到游戏、采集失败、连接失败、输入错误、撤回结果等。
- 显示：标题、说明、确定按钮。
- 这是共用模板，不代表多个不同界面。

### UI-09 自动更新已就绪窗口

- 显示：新版本号与更新说明。
- 操作：立即更新、稍后处理。
- 仅测试环境验证通过后才能进入正式发布流程。

### UI-10 当前游戏角色输入窗口

- 仅在窗口标题和绿色角色名都没有识别结果时出现。
- 绿色角色名能够匹配唯一网页角色时，跳过本界面并直接进入 UI-12。
- 识别到新角色名时，跳过本界面并直接进入 UI-11 创建/绑定。
- 显示绿色文字识别候选，并允许人工输入当前角色名。
- 操作：确认、取消。

### UI-11 网页角色绑定及创建窗口

- 仅在无法按同名角色自动匹配、历史绑定无效，或用户主动更换绑定时出现。
- 操作：从列表绑定已有角色；或直接选择目标账号，将识别到的游戏角色名创建并绑定；取消。
- 不再要求手工输入账号名称或再次输入角色名称。

### UI-12 确认采集内容窗口

- 显示：识别角色、实际写入位置、数据项、变更前数值、变更后数值、带符号变化值、5 秒倒计时。
- 数值未变化：变更前和变更后均使用正常字体。
- 数值有变化：仅变更后数值使用黄色；人工修改时颜色实时更新。
- 变化值：正数使用绿色、负数使用红色、无变化使用弱化色；各数字列保持统一左对齐。
- 变化列只负责展示，实际提交数据始终取可编辑的“变更后”数值，不改变现有接口字段与写入事务结构。
- 操作：更换绑定、立即写入、取消。
- 可信度不在界面中展示，但仍参与内部安全判断；角色不一致、识别可靠性不足或数值变化异常时暂停自动写入。

### UI-13 采集成功及撤回窗口

- 显示：当前角色、已写入字段、8 秒倒计时。
- 操作：撤回本次写入。
- 8 秒结束后自动关闭；撤回只针对本次采集事务。

### UI-14 共用组件与自适应规则

- 统一标题栏、按钮、输入框、滚动条、状态颜色、间距和字号。
- 覆盖不同分辨率、Windows 缩放比例、长角色名和长账号名。
- 所有关键按钮必须完整可见，窗口不得超出当前显示器工作区。

#### UI-11A 创建新角色时的账号输入状态

- 从 UI-11 的“创建新角色”进入。
- 显示已有账号名称，输入目标账号名称。
- 操作：确认、取消。

## 二、完整流程编号

### FLOW-01 启动与配对

1. 启动程序，显示 UI-01、UI-02、UI-03。
2. 未配对：UI-06 → UI-07 或 UI-08。
3. 已配对：直接检测游戏窗口和当前角色。

### FLOW-02 正常采集

1. 点击 UI-03。
2. UI-03 显示“正在识别…”，随后暂时隐藏。
3. 截取游戏窗口，不上传、不保留原始截图。
4. 识别当前画面的有效字段：
   - 远征界面：白奥德、蓝奥德（存在时）、基纳。
   - HUD 道具模式：道具等级、基纳。
   - HUD 战力模式：战斗力、基纳。
5. 解析角色：自动匹配成功直接继续；无法匹配时进入 UI-10/UI-11。
6. 显示 UI-12。
7. 倒计时结束或点击“立即写入”。
8. 写入云端并生成事务编号。
9. 显示 UI-13；8 秒内允许撤回本次事务。
10. 结束后恢复 UI-03。

### FLOW-03 更换角色绑定

1. 在 UI-12 点击“更换绑定”。
2. 进入 UI-11。
3. 选择已有角色或通过 UI-11A 创建角色。
4. 返回 UI-12，重新显示写入位置。

### FLOW-04 取消与异常恢复

1. 识别期间可在 UI-01 或 UI-02 选择“取消当前采集”。
2. 超时、无有效数据或网络错误进入 UI-08。
3. 确认提示后恢复 UI-03，不应停留在“正在识别…”。

## 三、反馈格式

请使用以下格式反馈，便于准确定位：

`界面编号 + 分辨率/缩放比例 + 问题位置 + 期望结果`

示例：`UI-12，3840×2160、Windows 150% 缩放，底部“更换绑定”被裁切，期望三按钮完整显示。`
